<h1>Listing prompt_items</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @prompt_items.each do |prompt_item| %>
  <tr>
    <td><%= link_to 'Show', prompt_item %></td>
    <td><%= link_to 'Edit', edit_prompt_item_path(prompt_item) %></td>
    <td><%= link_to 'Destroy', prompt_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Prompt item', new_prompt_item_path %>
