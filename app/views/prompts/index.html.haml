%html
  %body>
    .container
      .logoHolder
        %img{:src => "/logo.jpg"}

        .section.heading A film festival for people without patience
			
      .divider
			
      %ul.section
        %li A new festival every hour on the hour
        %li You have one hour to make your movie, and post it with that festival’s hashtag
        %li The films with the most likes one hour later win

      .divider

      .section= "Current Festival: #{@prompts[0].get_tweet_text}"

      .divider

      .section= "#{@prompts[1].pretty_timestamp} Festival: #{@prompts[1].get_tweet_text}"

      .divider

      .section= "#{@prompts[2].pretty_timestamp} Festival: #{@prompts[2].get_tweet_text}"

      %div{:style => "margin-left:72px;height: 240px;"}
        - @prompts[2].winners.each do |winner|
          .videoHolder{:style => "float:left;margin-right:72px;"}
            %div
              %a{:href => winner.vine_url}
              %iframe{:src => "#{winner.vine_url}/card", :width => "200", :height => "200"}
            %div
              %a.link{:href => "http://twitter.com/#{winner.user_name}"}
                = "@#{winner.user_name}"